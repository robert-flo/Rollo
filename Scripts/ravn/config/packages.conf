# RaVN Framework v1 — Package Registry
# Enable (true) or disable (false) individual task modules.
# If a package is not listed, it defaults to true.

# Examples:
# nvim-lazyman=false
# spicetify=true
# omarchy=false
# omarchy-repo=false
#
# Local development safety override (prevents hard reset of local changes)
# ravn=false
